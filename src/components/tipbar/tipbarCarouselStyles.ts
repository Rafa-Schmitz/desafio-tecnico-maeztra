import { createStyles } from "@mantine/core";

export const useTipBarCarouselStyles = createStyles(() => ({
  tipBar: {
    padding: "0 0 0 31px",
    maxWidth: "1600px",
    margin: "0 auto",
    userSelect: "none",
    cursor: "pointer"
  },
}));